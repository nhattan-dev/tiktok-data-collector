<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="persistence">

        <class>entity.Ad</class>
        <class>entity.AdGroup</class>
        <class>entity.Campaign</class>
        <properties>
            <property name="hibernate.connection.url"
                      value="jdbc:postgresql://localhost:5432/tiktok_data_collector?user=postgres&amp;password=sa"/>
            <property name="hibernate.connection.driver_class" value="org.postgresql.Driver"/>
<!--            <property name="hibernate.show_sql" value="true"/>-->

            <!-- JDBC connection pool (use the built-in) -->
            <property name="connection.pool_size" value="4"/>

            <!-- SQL dialect -->
            <property name="dialect" value="org.hibernate.dialect.PostgreSQL94Dialect"/>

            <!-- Enable Hibernate's automatic session context management -->
            <property name="current_session_context_class" value="thread"/>

            <!-- Show all executed SQL to console -->
            <!-- Enable tính năng Batch processing của Hibernate. -->
            <property name="jdbc.batch_size" value="10"/>
            <!-- Nói với Hibernate rằng hãy order câu lệnh INSERT và UPDATE tương tự nhau để improve performance.-->
            <property name="jdbc.order_inserts" value="true"/>
            <property name="jdbc.order_updates" value="true"/>

            <!-- Sử dụng để theo thống kê số lượng SQL được thực thi, batch processing.-->
            <property name="generate_statistics" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
